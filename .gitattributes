# 1. 모든 텍스트 파일의 줄바꿈을 Git이 자동 관리 (OS에 맞춰 변환)
* text=auto

# 2. 실행 스크립트는 항상 LF로 고정 (Unix/Linux/macOS 호환)
/gradlew text eol=lf
*.sh text eol=lf

# 3. 윈도우 배치 파일은 항상 CRLF로 고정
*.bat text eol=crlf
*.cmd text eol=crlf

# 4. 바이너리 파일은 변환하지 않음
*.jar binary
*.png binary
*.jpg binary
