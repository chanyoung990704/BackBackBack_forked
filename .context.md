# Development Context (Integrated)

## 1. Current Goals (현재 목표)

### A. 도메인/엔티티 인프라

* [ ] **ERD 기반 키/제약 수기 확정**

    * PK/FK
    * Unique / Index
    * Cascade / Nullable 정책
* [ ] **신규 엔티티 인프라 준비**

    * 패키지 구조 확정
    * BaseEntity / Auditing 전략 정리
* [ ] **기본 스캐폴딩**

    * Repository / Test 기본 골격

### B. 인증/보안 (JWT Phase 2) [COMPLETED]

* [x] **비밀번호 만료 정책 (90일)**
* [x] **로그아웃 / 전체 로그아웃**
* [x] **Access Token 블랙리스트**
* [x] **Refresh Token Rotation + 동시성 제어**
* [x] **refresh_tokens 테이블**

### C. 포트폴리오 최적화 [COMPLETED]

* [x] **포트폴리오용 README.MD 비즈니스 관점 전면 개편 완료**
* [x] **Transactional Outbox 및 Custom Saga Orchestration 패턴 반영 완료**
* [x] **README.MD 면접관 최적화 전면 재정렬 및 기여도 독립 격상 완료**
* [x] **README.MD 내 데이터베이스 인덱싱 최적화 5대 사례 연동 및 하이브리드 Fallback 단독 트러블슈팅 문서화 완료**
* [x] **포트폴리오 프리미엄 가이드라인 (PORTFOLIO_GUIDE.md) 신설 및 README.md 벤치마크/Gantt 차트 시각화 보완 완료**


---

## 2. Recent Changes (최근 변경 사항)

### 2026-06-23

* `feat(modulith)` Spring Modulith 점진적 도입 및 패키지 구조 재구조화 (Bounded Context에 맞추어 `auth`, `company`, `post`, `file` 등 Bounded Context 단위로 분리)
* `refactor(modulith)` 모듈 간 순환 참조를 제거하여 단방향 의존성 구성 및 `ModulithVerificationTest` 아키텍처 검증 구현
* `test(modulith)` Modulith 아키텍처 다이어그램(PlantUML) 자동 문서화 설정 및 전체 416개 비즈니스 통합 테스트 100% 그린 패스(BUILD SUCCESSFUL) 입증

### 2026-06-04

* `fix(batch)` [DartCorpCodeItemReader.java](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/src/main/java/com/aivle/project/company/batch/DartCorpCodeItemReader.java)의 `open` 및 `update` 메서드 파라미터에 `@NonNull` 애너테이션을 명시해 부모 인터페이스(ItemStream) 규약 불일치로 발생하는 컴파일 오류 수정
* `fix(batch)` [DartCorpCodeItemProcessor.java](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/src/main/java/com/aivle/project/company/batch/DartCorpCodeItemProcessor.java)의 `process` 메서드 파라미터에 `@NonNull` 애너테이션을 명시해 부모 인터페이스 규약 위반으로 발생하는 컴파일 에러 수정
* `fix(batch)` [AiJobKafkaConsumer.java](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/src/main/java/com/aivle/project/company/job/AiJobKafkaConsumer.java)에 누락된 `CompanyAiReportStoreService` 임포트 구문을 추가하여 `cannot find symbol` 컴파일 오류 해결
* `refactor(batch)` [DartCorpCodeItemWriter.java](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/src/main/java/com/aivle/project/company/batch/DartCorpCodeItemWriter.java)의 `write` 메서드를 데이터 흐름 단위로 문단을 나누고, 한글 주석을 추가하여 가독성을 높임

### 2026-05-26

  * 독립 React (Vite + Tailwind CSS v4) 프론트엔드 대시보드 구축 및 Vite+PostCSS 빌드(npm run build) 100% 성공 검증 완료 (App.jsx, Dashboard.jsx, QnaBoard.jsx, SagaProgressVisualizer.jsx)
  * 포트폴리오 가이드 및 `README.md` 2차 하드닝(SLO, P95/P99 퍼센타일, 디그레이드 아키텍처 융합) 및 가상 스레드 꼬리 지연 원인 분석 정밀화 완료
  * 포트폴리오 프리미엄 가이드라인 (`PORTFOLIO_GUIDE.md`) 신설 및 메인 `README.md` 내 성능 벤치마크 데이터 테이블 및 Mermaid Gantt 차트 시각화 보완 완료
  * Windows/Linux용 가상환경 셋업 및 로컬 실행 스크립트 (`setup_env.ps1`, `setup_env.sh`) 및 상세 한글 안내서 (`README.md`) 작성 완료

### 2026-05-22

* `docs` [README.md](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/README.md)에 실제 데이터베이스 인덱스 4대 최적화 사례 분석 및 "5️⃣ 대규모 기업 검색 및 지표 룩업 성능 극대화를 위한 다중 인덱스 튜닝 및 H2 하이브리드 Fallback" 단독 트러블슈팅 문서화 완료
* `docs` 포트폴리오용 [README.md](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/README.md) 상단 Executive Summary 표 5번째 행 추가 및 Backend Leader 기여도 튜닝 파트 갱신 완료
* `docs` [task.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/task.md) 및 [walkthrough.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/walkthrough.md)에 인덱스 최적화 태스크 및 검증 상세 갱신 완료
* `docs` [README.MD](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/README.MD)에 인프라 보안 및 SWOT/VRIO 관점의 비즈니스 가치 분석 내용 추가 완료 (AWS/듀얼클라우드 네트워크 및 보안, SWOT/VRIO 경쟁 우위 검증)
* `docs` 포트폴리오용 [README.MD](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/README.MD) 면접관 최적화 재정렬 완료 (기여도 최상단 단독 격상, 아키텍처 다이어그램 및 4대 트러블슈팅 전면 배치)
* `docs` [task.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/task.md), [walkthrough.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/walkthrough.md) 최종 갱신 및 마크다운/Mermaid 6개 렌더링 검증 완료

### 2026-05-21

* `docs` 포트폴리오용 비즈니스 지향 [README.MD](file:///c:/Users/chanyoungpark/.gemini/antigravity/scratch/BackBackBack_forked/README.MD) 개편 완료 (1차: 성능/비용 최적화, 2차: Transactional Outbox 및 Custom Saga Orchestration 융합 완료)
* `docs` [task.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/task.md), [walkthrough.md](file:///C:/Users/chanyoungpark/.gemini/antigravity/brain/7fc71ee9-cb6e-4670-96dc-6151429ace22/walkthrough.md) 작성 및 검증 최종 완결

### 2026-03-04

* `fix(config)` `src/main/resources/application.yaml`의 최상위 `server` 중복 키와 `spring.*` 오들여쓰기 구조를 정리해 Spring 컨텍스트 초기화 실패(`Failed to load ApplicationContext`) 재발 경로를 제거
* `fix(ci)` `buildspec.yml` PRE_BUILD 단계에 Python 기반 YAML 중복 키/파싱 검증을 추가해 설정 파일 구조 오류를 테스트 실행 전에 명확한 메시지로 fail-fast 처리
* `fix(ci)` CodeBuild 테스트 단계에 `SPRING_PROFILES_ACTIVE=test`를 명시해 CI 실행 시 테스트 프로파일(`application-test.yaml`)이 일관 적용되도록 보정
* `fix(ci)` 테스트 실패 시 `build/test-results/test` XML의 failure/error 라인을 로그로 덤프하도록 `buildspec.yml`을 보강해 CodeBuild 로그만으로 실패 테스트 원인을 추적 가능하게 개선
* `fix(ci)` 이미지 빌드가 실패한 경우 POST_BUILD에서 docker push 단계를 조건부 스킵하도록 보강해 2차 연쇄 오류(`No such image`) 노이즈를 제거

### 2026-02-26

* `fix(ai)` `CompanyAiService.processReportGeneration`의 기존 보고서 존재 확인 경로를 엔티티 프록시 접근(`getReportFileById` self-invocation)에서 파일 ID 직접 조회 쿼리(`CompanyReportVersionsRepository.findLatestPdfFileIdsByCompanyIdAndYearAndQuarter`)로 전환해 `FilesEntity` LazyInitializationException(`no session`) 재발 경로를 제거
* `test(ai)` `CompanyAiReportStoreServiceTest`, `AiJobKafkaConsumerTest`를 추가하고 auth/ai/report/watchlist 관련 타깃 테스트 10종을 재실행해 통과 확인
* `fix(ai)` `AiServerClient`의 리포트 다운로드 경로를 `ResponseEntity<byte[]>` 기반으로 변경하고 `Content-Type=application/pdf`를 강제 검증해 비정상 응답(JSON/텍스트) 처리 시 조기 실패하도록 보강
* `refactor(ai)` `CompanyAiService`에서 AI PDF 다운로드(외부 호출)와 파일 저장/버전 링크(DB 쓰기)를 분리하고 `storeAndLinkReport(@Transactional)`를 도입해 긴 외부 호출 동안 DB 트랜잭션 점유를 피하도록 구조 개선

### 2026-02-25

* `refactor(insight)` `CompanyInsightService`를 쓰기 단계(`ensureInsightData`, `@Transactional`)와 조회 단계(`loadInsights`, `@Transactional(readOnly = true)`)로 분리해 인사이트 조회 흐름의 트랜잭션 경계를 명확화
* `fix(insight)` `CompanyInsightController`, `AdminCompanyQueryController`, `PerfBenchmarkController`가 `ensureInsightData -> loadInsights` 순서로 오케스트레이션하도록 변경해 지연 로딩 시 세션 종료 예외 재발 가능성을 완화
* `test(insight)` 인사이트/관리자/perf 컨트롤러 테스트를 신규 메서드 호출 체인에 맞춰 갱신하고 타깃 테스트 4종(`CompanyInsightServiceTest`, `CompanyInsightControllerTest`, `AdminCompanyQueryControllerTest`, `PerfBenchmarkControllerTest`) 통과

### 2026-02-18

* `fix(company)` `CompanyOverviewService`에서 quarterKey 미지정 시 ACTUAL 분기가 없는 기업도 예외 대신 빈 개요(회사 기본정보 + 빈 forecast/signals/keyMetrics)로 응답하도록 fallback 처리
* `refactor(logging)` `ApiLoggingAspect`의 무거운 마스킹/문자열 가공을 `ApiLogProcessor` 비동기 컴포넌트로 분리하고 AOP는 요청 메타데이터 수집/위임만 담당하도록 단순화
* `test(logging)` `ApiLoggingAspectTest`를 신규 로깅 구조(`ApiLogProcessor`) 기준으로 정합화해 마스킹 회귀를 검증하고 CI와 동일한 Gradle 테스트 명령 통과 확인
* `chore(config)` `.env.example`/`docker-compose.app.yml`/`docker-compose.local.yml`/`README.MD`에 `SPRING_THREADS_VIRTUAL_ENABLED`, `APP_VIRTUAL_THREAD_*`, `APP_AI_JOB_KAFKA_ENABLED` 가이드를 추가해 운영/로컬 환경의 Kafka+VirtualThread 토글 경로를 명시

### 2026-02-17

* `fix(deploy)` docker compose 공통 실행기(`run_compose_command`)가 `image-uri.env`를 자동 로드하도록 보강해 `APP_IMAGE` 누락으로 `compose ps/logs`가 실패하는 운영 이슈를 방지
* `fix(deploy)` `health-check.sh`에서 Kafka 활성화(`APP_AI_JOB_KAFKA_ENABLED=true`) 시 `kafka` 서비스 정의/상태를 필수 검증하도록 강화해 구버전 `docker-compose.app.yml` 배포를 ValidateService 단계에서 즉시 실패 처리

### 2026-02-16

* `feat(async/kafka)` 이슈 #127 1차로 AI 장시간 작업 Kafka 경로를 추가: `AiJobDispatchService`/`AiJobKafkaConsumer`/`AiJobMessage`/`AiJobType`/`KafkaTopicConfig`를 도입하고, `CompanyAiController` 리포트 요청 및 `CompanyWatchlistAsyncHandler` AI 코멘트 워밍업을 Kafka 우선 + 기존 경로 fallback 구조로 전환
* `chore(docker)` 기존 compose(`docker-compose.yml`, `docker-compose.local.yml`, `docker-compose.app.yml`)에 Kafka(bitnami/kafka, KRaft 단일노드) 서비스를 추가하고 앱 env에 `SPRING_KAFKA_BOOTSTRAP_SERVERS`, `APP_AI_JOB_KAFKA_ENABLED`를 연결
* `chore(config)` `application.yaml`/`application-dev.yaml`에 Kafka 및 AI Job 토픽/토글 설정을 추가
* `test(async/kafka)` `CompanyAiControllerTest`, `CompanyWatchlistAsyncHandlerTest`, `CompanyAiServiceTest` 타깃 테스트 통과
* `chore(config)` AI 외부 호출 기본 타임아웃을 운영 지연 허용값으로 상향(`connect 5s`, `response 15s`, `call 20s`)
* `fix(ai/error)` `ExternalAiUnavailableException`을 도입하고 AI 클라이언트(`AiServerClient`, `NewsClient`)에서 timeout/circuit-open/unavailable reasonCode를 표준화해 전파
* `fix(api)` `CommonErrorCode.COMMON_503` 및 `GlobalExceptionHandler` 매핑을 추가해 외부 AI 장애를 503으로 응답 가능하도록 정비
* `fix(insight)` `CompanyInsightService`에 외부 호출 실패 fallback을 적용해 기존 DB 인사이트가 있으면 응답 유지, 데이터가 전혀 없을 때만 503 예외 전파
* `refactor(logging)` `CompanyWatchlistAsyncHandler` 실패 로그에 `operation/companyId/stockCode/reasonCode` 구조화 필드를 추가하고 외부 AI 장애 reasonCode 분류를 강화
* `test(ai/insight/watchlist)` `AiServerClientTest`, `NewsClientTest`, `CompanyInsightServiceTest`, `CompanyWatchlistAsyncHandlerTest`를 보강해 예외 매핑/fallback/비동기 실패 흡수 시나리오 검증
* `test(auth)` `AuthIntegrationTest`의 `change-password`/`logout-all` 시나리오에 CSRF 쿠키+헤더 전달을 반영해 현재 보안 정책과 정합화하고 `./gradlew test` 전체 통과 확인

### 2026-02-14

* `fix(test)` CodeBuild `compileTestJava` 실패 대응: `CommentsService.listByPost` 시그니처 변경(Long 단일 인자)에 맞춰 댓글 서비스/통합 테스트 호출부를 정합화하고, `AuthIntegrationTest`의 `TokenHashService` import 누락 및 `PostMapperTest`의 구 role-mocking 코드를 현재 `PostMapper` 구현에 맞게 수정
* `fix(ci)` upstream main 머지 커밋 기준 CodeBuild 컴파일 오류 보정: `CommentController`의 `commentsService.listByPost` 호출 인자를 서비스 시그니처(`Long`)에 맞게 수정하고 `PostMapper`의 미완성/미사용 `isAdminComment` 블록을 제거해 `UserRoleEntity`/`userRoleRepository`/`RoleName` 심볼 오류를 해소
* `fix(comment)` CodeBuild 컴파일 실패 대응: `CommentsRepository`에 `findByPostIdAndDeletedAtIsNullOrderByDepthAscSequenceAsc`를 추가하고 `CommentsService.listByPost` 호출 시그니처를 정합화해 `cannot find symbol` 오류를 해소
* `refactor(security)` `NameMaskingUtil`을 추가하고 `PostMapper`/`CommentMapper`/`UserMapper`/`AdminUserQueryService`에 사용자명 마스킹을 적용해 게시글/댓글/QnA/로그인 응답/관리자 사용자 목록의 실명 노출을 제한
* `feat(auth)` `LoginAttemptProperties`/`LoginAttemptService`를 도입하고 `AuthService.login`에 로그인 실패 누적(5회) 및 잠금(기본 15분) 제어를 연동, `AUTH_429` 에러 코드와 로그인 API 429 문서화 추가
* `test(auth/security)` `LoginAttemptServiceTest`/`AuthServiceTest`/`AuthIntegrationTest`를 포함한 타깃 테스트 통과, `./gradlew cleanTest test`는 1회차 XML 결과 파일 쓰기 오류 후 재실행에서 전체 통과 확인
* `feat(resilience)` 이슈 #113으로 AI 외부 호출 경로에 전용 `aiWebClient`(connect/response/call timeout + pool)와 Resilience4j(circuit breaker/retry/bulkhead)를 적용하고, 서비스 내 `Thread.sleep` 기반 수동 재시도를 제거
* `perf(metrics)` 벤치마크 비교 스크립트에 API/런타임 전후 변화율 CSV(`comparison-api-delta.csv`, `comparison-runtime-delta.csv`)를 추가하고 `resilience4j` 메트릭 수집 컬럼을 확장해 정량 보고 산출물을 보강
* `refactor(post/perf)` QnA `qnaStatus` 계산을 role 기반 쿼리로 전환해 N+1/비효율 조회를 줄이고, 대시보드 위험도 요약을 projection/pageable 기반 쿼리로 최적화
* `fix(logging)` 이슈 #111로 `ApiLoggingAspect`에 JWT/Bearer/Cookie 문자열 패턴 마스킹을 추가하고 `@CookieValue` 파라미터를 로깅에서 `"[COOKIE_MASKED]"`로 강제 비노출 처리
* `test(logging)` `ApiLoggingAspectTest`에 Bearer/JWT/Cookie 마스킹 및 `@CookieValue` 인자 비노출 케이스를 추가하고 타깃 테스트+전체 테스트를 통과
* `fix(logging)` 이슈 #111로 `ApiLoggingAspect`에 JWT/Bearer/Cookie 문자열 패턴 마스킹을 추가하고 `@CookieValue` 파라미터를 로깅에서 `"[COOKIE_MASKED]"`로 강제 비노출 처리
* `test(logging)` `ApiLoggingAspectTest`에 Bearer/JWT/Cookie 마스킹 및 `@CookieValue` 인자 비노출 케이스를 추가하고 타깃 테스트+전체 테스트를 통과
* `fix(security)` `TokenHashService`를 SHA-256에서 pepper 기반 HMAC-SHA256으로 전환하고 `app.security.token-hash.pepper-base64(APP_TOKEN_HASH_PEPPER_B64)` 미설정/오형식 시 Fail-Fast 처리 추가
* `fix(auth)` `RefreshTokenService`/`EmailVerificationService` 토큰 조회 경로에 신규 HMAC 해시 우선 + 레거시 SHA-256 fallback 및 성공 시 신규 해시로 자동 마이그레이션 적용
* `test(auth/security)` `TokenHashServiceTest`, `RefreshTokenServiceTest` 보강 및 `./gradlew cleanTest test` 전체 통과
* `docs(security)` `docs/security-hardening-issue-109.md` 작성, `README.MD`/`.env.example`에 `APP_TOKEN_HASH_PEPPER_B64` 가이드 반영
* `feat(security)` QnA 보안 하드닝 1차: 파일/댓글 목록 조회에 게시글 읽기 정책(QnA 소유자 + soft delete) 검증을 적용하고 관련 단위/통합 테스트를 통과
* `feat(auth)` 인증 재전송 API 전환: `POST /api/auth/resend-verification` 인증 기반 경로를 추가하고 legacy `GET` 경로에 Deprecation/Sunset 헤더 및 유예 로그를 적용
* `feat(auth)` 인증 메일 재전송 남용 방지: Redis 기반 사용자/IP 레이트리밋(`VerificationResendRateLimiter`)을 추가
* `feat(auth)` refresh/email verification 토큰 저장구조를 SHA-256 해시 기반으로 전환하고 MySQL/H2 `V27__hash_auth_tokens` 마이그레이션을 추가
* `feat(auth)` `/api/auth/refresh`, `/api/auth/logout`에 더블서브밋 CSRF 검증(`csrf_token` + `X-CSRF-Token`)을 적용하고 CORS 허용 헤더를 확장
* `test(auth)` `EmailVerificationControllerTest`, `EmailVerificationServiceTest`, `RefreshTokenServiceTest`, `AuthIntegrationTest` 보강 후 `./gradlew cleanTest test` 전체 통과
* `docs(security)` `docs/security-hardening-issue-107.md`에 변경 내용/검증 결과/legacy 제거 일정(2026-02-28) 문서화

### 2026-02-13

* `chore(deploy)` Docker 전환용 배포 스크립트 공통 라이브러리(`scripts/lib/deploy-runtime.sh`)를 추가하고 `DEPLOY_RUNTIME(systemd|docker)` 분기 로직을 `setup/install/start/stop/health-check`에 반영
* `test(deploy)` 배포 런타임 helper 단위 검증 스크립트(`scripts/tests/deploy-runtime-test.sh`)를 추가하고 셸 검증(`bash -n`) 및 테스트를 통과
* `chore(deploy)` 운영 환경 변수 예시에 Docker 전환 변수(`DEPLOY_RUNTIME`, `COMPOSE_FILE_PATH`, `IMAGE_ENV_FILE`, `REDIS_HOST_DOCKER`)를 추가
* `chore(ci)` `buildspec.yml`을 Docker 이미지 빌드/태깅/ECR 푸시 및 `image-uri.env` 아티팩트 생성 방식으로 전환
* `chore(deploy)` `Dockerfile`, `.dockerignore`, `docker-compose.app.yml`를 추가해 CodeDeploy가 Compose로 `app+redis`를 기동하도록 준비
* `docs(deploy)` `README.MD`에 Docker 배포 전환(MVP) 절차, 서버 env 핵심값, 배포 검증 포인트를 문서화
* `chore(deploy)` 로컬 개발 편의를 위해 `docker-compose.local.yml`를 추가하고 base compose의 env 파일 경로를 `APP_ENV_FILE` 변수화해 운영/로컬 조합 실행을 지원
* `fix(ci)` CodeBuild Docker build 실패 대응: `.dockerignore`에서 `build/libs/app.jar`만 허용하도록 조정하고, `buildspec.yml`에서 non-plain bootJar를 `build/libs/app.jar`로 복사 후 이미지 빌드하도록 보강
* `fix(ci)` CodeBuild `YAML_FILE_ERROR` 대응: `buildspec.yml`의 콜론(`:`) 포함 commands 항목을 YAML-safe 단일 문자열로 감싸 `DOWNLOAD_SOURCE` 단계 파싱 실패를 제거
* `fix(ci)` 최소 변경 안전망 적용: `buildspec.yml` artifacts에 `build/libs/app.jar`를 추가해 `DEPLOY_RUNTIME=systemd` 전환 시에도 JAR 아티팩트 누락으로 배포가 실패하지 않도록 보강

### 2026-02-12

* `test(company/user)` role 시드 파급 회귀 보정: `CompanyOverviewServiceTest`에 `CompanyReportVersionIssueService` mock bean 추가, `CustomUserDetailsServiceTest`를 role 시드 조회 방식으로 전환
* `chore(db)` `V26__seed_roles`(MySQL/H2)를 추가해 `ROLE_USER/ROLE_ADMIN/ROLE_ANALYST` role 시드를 멱등하게 고정
* `fix(auth)` `SignUpService`에서 회원가입 저장 구간의 `DataIntegrityViolationException`을 `EMAIL_ALREADY_EXISTS(409)` 도메인 에러로 일관 변환
* `fix(user)` `UserDomainService`의 역할 동적 생성 경로를 제거하고 role 시드 미존재 시 명시적 실패로 전환
* `test(auth)` `SignUpServiceTest`/`UserDomainServiceTest`/`AuthIntegrationTest`/`CompanyWatchlistServiceTest`/`UserRepositoryTest`로 role 시드 고정 및 signup duplicate 회귀 검증
* `fix(report)` `CompanyReportVersionIssueService`를 추가해 report row lock 기반 버전 발급을 공통화하고, report import/publish/predict/pdf + `CompanyAiService`의 버전 생성 경로를 단일 호출로 통합
* `fix(report)` `GetOrCreateResolver` 공통 유틸을 도입해 분기/리포트 `getOrCreate` 경합 시 `DataIntegrityViolationException` 이후 재조회로 수렴하도록 패턴 통일
* `test(report)` `GetOrCreateResolverTest` 및 report 서비스/`CompanyAiService`/`CompanyPredictionCacheService` 타깃 테스트를 통과해 동시성 회귀 검증
* `fix(company)` `CompanyAiService` AI 리포트 파일 조회를 "최신 버전"이 아닌 "PDF가 있는 최신 버전" 기준으로 변경해, 최신 버전이 `pdfFile = null`이어도 기존 PDF를 재사용하도록 보정
* `fix(company)` `generateReportAsync`에서 기존 PDF가 발견되면 `PROCESSING` 단계 없이 즉시 `COMPLETED`로 상태 전환하도록 흐름 최적화
* `test(company)` `CompanyAiServiceTest`에 기존 PDF 재사용 및 비동기 즉시 완료 상태 전이 검증 테스트 추가, 관련 타깃 테스트 통과

### 2026-02-11

* `chore(perf)` Phase1 벤치마크 스크립트에 런타임 계측(Actuator metrics) 수집/요약 파이프라인을 추가하고 `comparison-summary.md`에 before/after 런타임 지표 비교 표를 포함하도록 확장
* `fix(perf)` perf 실행 중 서버 준비/종료 검증을 강화하고 동일 run_id 재실행 시 JMeter 결과 충돌(HTML/JTL 잔존) 방지 로직을 추가
* `fix(security)` perf 프로파일에서 `/actuator/metrics` 접근을 허용해 벤치마크 계측이 401 없이 수집되도록 보강
* `docs(perf)` `docs/perf-phase1-virtual-thread-benchmark.md`에 runtime metrics 산출물 및 비교 결과 포함 사항을 반영

* `fix(perf)` `CompanyAiService`의 AI 리포트 버전 생성 경로에 `company_reports` 비관적 락(`findByIdForUpdate`)을 적용해 `company_report_versions(company_report_id, version_no)` 동시성 충돌(500)을 완화
* `fix(perf)` `PerfDataInitializer`가 벤치마크 기준 분기(2026 Q1)의 `quarters`/`company_reports`를 선생성하도록 보강해 초기 생성 레이스를 감소
* `test(perf)` `CompanyAiServiceTest`, `PerfBenchmarkControllerTest` 통과 및 perf 프로필 병렬 호출(100회, 동시 20)에서 `HTTP 200` 100건/유니크 충돌 로그 0건 확인
* `feat(async)` Virtual Thread 토글(`app.virtual-thread.*`) 및 `spring.threads.virtual.enabled` 설정을 추가하고, `insightExecutor`/`emailExecutor`가 Virtual Thread와 기존 ThreadPool fallback을 선택하도록 보강
* `feat(perf)` perf 프로파일(`application-perf.yaml`)과 perf 데이터 초기화, 성능 벤치마크 API(`/api/perf/benchmark/*`)를 추가
* `feat(perf)` AI/Turnstile 외부 의존성 mock 모드(`ai.server.mock.*`, `turnstile.mock-allow-all`)를 도입해 부하 측정 재현성을 확보
* `chore(perf)` JMeter 플랜/프로퍼티/실행 스크립트/비교 스크립트(`perf/jmeter`, `perf/scripts`)와 벤치마크 가이드 문서(`docs/perf-phase1-virtual-thread-benchmark.md`)를 추가
* `test(perf)` `InsightExecutorConfigTest`, `PerfBenchmarkControllerTest`, `NewsClientTest` 보강 및 `./gradlew test` 전체 통과

* `fix(auth)` `logout-all` 호출 시 사용자 refresh 토큰을 Redis 세션키/토큰키 및 DB 활성 토큰에서 일괄 폐기하도록 보강
* `fix(auth)` `refresh` 경로에 logout-all 기준 시각 검증을 추가해, 전체 로그아웃 이전 토큰 재발급 차단
* `refactor(auth)` refresh 토큰 캐시 시간값을 밀리초 기준으로 정비하고 레거시(초 단위) 캐시 호환 처리, `RefreshTokenService` 트랜잭션 경계 보강
* `test(auth)` `AuthServiceTest`, `RefreshTokenServiceTest`, `AuthIntegrationTest`에 logout-all 이후 refresh 거부/레거시 캐시 호환/전체 세션 폐기 시나리오 추가, `./gradlew test` 통과
* `chore(github)` upstream 이슈 #93 생성 및 PR #94 생성

* `chore(perf)` Phase1 벤치마크 스크립트에 런타임 계측(Actuator metrics) 수집/요약 파이프라인을 추가하고 `comparison-summary.md`에 before/after 런타임 지표 비교 표를 포함하도록 확장
* `fix(perf)` perf 실행 중 서버 준비/종료 검증을 강화하고 동일 run_id 재실행 시 JMeter 결과 충돌(HTML/JTL 잔존) 방지 로직을 추가
* `fix(security)` perf 프로파일에서 `/actuator/metrics` 접근을 허용해 벤치마크 계측이 401 없이 수집되도록 보강
* `docs(perf)` `docs/perf-phase1-virtual-thread-benchmark.md`에 runtime metrics 산출물 및 비교 결과 포함 사항을 반영

* `fix(perf)` `CompanyAiService`의 AI 리포트 버전 생성 경로에 `company_reports` 비관적 락(`findByIdForUpdate`)을 적용해 `company_report_versions(company_report_id, version_no)` 동시성 충돌(500)을 완화
* `fix(perf)` `PerfDataInitializer`가 벤치마크 기준 분기(2026 Q1)의 `quarters`/`company_reports`를 선생성하도록 보강해 초기 생성 레이스를 감소
* `test(perf)` `CompanyAiServiceTest`, `PerfBenchmarkControllerTest` 통과 및 perf 프로필 병렬 호출(100회, 동시 20)에서 `HTTP 200` 100건/유니크 충돌 로그 0건 확인
* `feat(async)` Virtual Thread 토글(`app.virtual-thread.*`) 및 `spring.threads.virtual.enabled` 설정을 추가하고, `insightExecutor`/`emailExecutor`가 Virtual Thread와 기존 ThreadPool fallback을 선택하도록 보강
* `feat(perf)` perf 프로파일(`application-perf.yaml`)과 perf 데이터 초기화, 성능 벤치마크 API(`/api/perf/benchmark/*`)를 추가
* `feat(perf)` AI/Turnstile 외부 의존성 mock 모드(`ai.server.mock.*`, `turnstile.mock-allow-all`)를 도입해 부하 측정 재현성을 확보
* `chore(perf)` JMeter 플랜/프로퍼티/실행 스크립트/비교 스크립트(`perf/jmeter`, `perf/scripts`)와 벤치마크 가이드 문서(`docs/perf-phase1-virtual-thread-benchmark.md`)를 추가
* `test(perf)` `InsightExecutorConfigTest`, `PerfBenchmarkControllerTest`, `NewsClientTest` 보강 및 `./gradlew test` 전체 통과

* `fix(auth)` `logout-all` 호출 시 사용자 refresh 토큰을 Redis 세션키/토큰키 및 DB 활성 토큰에서 일괄 폐기하도록 보강
* `fix(auth)` `refresh` 경로에 logout-all 기준 시각 검증을 추가해, 전체 로그아웃 이전 토큰 재발급 차단
* `refactor(auth)` refresh 토큰 캐시 시간값을 밀리초 기준으로 정비하고 레거시(초 단위) 캐시 호환 처리, `RefreshTokenService` 트랜잭션 경계 보강
* `test(auth)` `AuthServiceTest`, `RefreshTokenServiceTest`, `AuthIntegrationTest`에 logout-all 이후 refresh 거부/레거시 캐시 호환/전체 세션 폐기 시나리오 추가, `./gradlew test` 통과
* `chore(github)` upstream 이슈 #93 생성 및 PR #94 생성

* `fix(user)` `GET /api/admin/users` 목록 조회에서 `ROLE_ADMIN` 보유 사용자 제외 조건 추가(`NOT EXISTS` 기반 역할 필터)
* `test(user)` 사용자 목록 조회 시 `ROLE_ADMIN`/복합 권한 사용자 제외를 검증하는 `UserRepositoryTest` 추가
* `fix(test)` CodeBuild `ApplicationContext` 실패 대응: 테스트 전용 RSA 키(`src/test/resources/jwt`)를 추가하고 `application-test.yaml`에 JWT 키 경로/issuer/kid 명시
* `chore(ci)` `buildspec.yml` 테스트 단계에서 `JWT_PRIVATE_KEY_PATH`/`JWT_PUBLIC_KEY_PATH`/`JWT_CURRENT_KID`를 테스트 키로 강제 지정해 CI 환경 의존성 제거

### 2026-02-10

* `fix(test)` CI 연쇄 실패 완화: `@ActiveProfiles("dev")` 테스트를 `test`로 통일하고 프로필 미지정 `@SpringBootTest`에 `test` 프로필 명시
* `fix(test)` test 프로필 공통 파일 스토리지 스텁(`TestFileStorageConfig`) 기준으로 파일 업로드 통합테스트 기대값/구성 정합화
* `chore(ci)` `buildspec.yml` 빌드 단계를 테스트(`clean test --stacktrace --info`)와 패키징(`bootJar -x test`)으로 분리

* `chore(db)` companies 이름 검색 FULLTEXT 인덱스 멱등 Flyway(V25) 분리 적용(MySQL) 및 H2 no-op V25 추가
* `fix(company)` 기업 검색에 FULLTEXT 우선 + 실패/0건 시 LIKE fallback 하이브리드 로직 적용, `stock_code IS NOT NULL` 조건 유지
* `test(company)` CompanySearch 하이브리드 fallback 단위 테스트 추가 (FULLTEXT 성공/0건 fallback/예외 fallback/H2 LIKE 경로)
* `chore(db)` companies 이름 검색용 FULLTEXT 인덱스 Flyway V25 추가(MySQL), H2는 no-op 마이그레이션 반영
* `refactor(post)` 게시글 사용자 API REST 경로 정규화 1차: 신 경로(`/api/posts?categoryName=...`, `/api/posts/{postId}`, `POST /api/posts`) 추가 및 구 경로 하위호환 유지
* `refactor(post)` `PostService`에 postId 기준 조회/수정/삭제 메서드(`getById`, `updateById`, `deleteById`) 추가
* `test(post)` `PostControllerIntegrationTest`에 신 경로 생성/목록/수정/삭제 테스트 4건 추가 및 통과
* `refactor(controller)` Company/AI/News/Report/File/Watchlist 컨트롤러에 REST 신 경로를 추가하고 구 경로 하위호환을 병행 지원
* `test(controller)` `CompanySearchControllerTest`, `CompanyOverviewTempControllerTest`, `CompanyAiControllerTest`, `FileDownloadControllerTest`, `CompanyWatchlistControllerTest`에서 신 경로 회귀 검증 통과
* `refactor(security)` `SecurityConfig` 매처를 공개/인증/USER 그룹 상수로 정리하고 신/구 URI를 동시 허용하도록 재배치
* `test(security)` `SecurityEndpointAuthorizationTest`로 공개 검색 permitAll, 사용자 경로 인증/인가, ADMIN 경로 USER 차단(403) 회귀 검증 통과
* `docs(api)` `docs/rest-endpoint-migration-2026-02.md`에 엔드포인트 Old->New 매핑 및 하위호환 정책 추가

### 2026-02-08

* `fix(company)` CompanyOverview ID/종목코드 해석 우선순위 보정
* `fix(report)` 보고서/지표 컨트롤러 재활성화 및 보안 경로 복구
* `fix(watchlist)` 워치리스트 비동기 캐시 처리에서 read-only 쓰기 오류 방지
* `feat(watchlist)` 워치리스트 등록 후 기업 개요 캐시를 비동기로 선행 적재(@EnableAsync + 이벤트 기반)
* `fix(auth)` @CurrentUser Long 주입을 userId 클레임 기반으로 지원하고 watchlist 컨트롤러 테스트 보정
* `feat(company)` 워치리스트 기업 목록 조회 API(`/api/companies`) 추가 및 CompanyInfo 응답/테스트 통과
* `feat(qna)` implemented QnA admin reply API (`/api/admin/posts/qna/{postId}/replies`) and dynamic `qnaStatus` ("pending"/"answered") logic in `PostMapper`.
* `refactor(post/comment)` PostResponse/CommentResponse author field transition from userId(Long) to name(String) and verification completed.

### 2026-02-09

* `fix(watchlist)` 워치리스트 소프트 삭제 후 동일 기업 재등록 시 중복 오류가 발생하지 않도록 활성 항목만 중복 체크하고, 삭제 이력은 restore+note 갱신으로 복구 처리
* `fix(company)` CompanyInfo reputationScore 응답을 100배 스케일링하고 소수 0자리 반올림 적용
* `feat(news)` 관리자 최신 뉴스 재수집 API(`/api/admin/companies/{stockCode}/news/refresh-latest`) 추가 및 `average_score` 누락 시 기사 점수 평균 복구 로직/테스트 반영
* `fix(news)` 최신 뉴스 재수집 API 권한을 ROLE_USER로 조정하고 경로를 `/api/companies/{stockCode}/news/refresh-latest`로 변경
* `feat(company)` AI 종합 코멘트 캐시 연동: `/api/v1/analysis/{company_code}/ai-comment` 클라이언트/서비스 추가, `company_key_metrics.ai_comment` 저장, 워치리스트 등록 시 선행 적재, 관리자 수동 캐시 API(`/api/admin/companies/{companyId}/ai-comment/cache`, `/api/admin/companies/ai-comment/cache`) 및 테스트 추가
* `fix(news)` 최신 뉴스 재수집 API 실행 후 `company_key_metrics.external_health_score`를 최신 분기로 즉시 동기화하도록 연결
* `feat(insight)` 인사이트 조회 API에 `refresh` 파라미터를 추가하고 refresh=true 시 뉴스/사업보고서 강제 재수집 및 `external_health_score` 동기화 반영

### 2026-02-08

* `fix(company)` CompanyOverview에서 종목코드 입력을 기업 ID로 해석하도록 보정
* `fix(company)` CompanyInfo overallScore를 composite_score로 매핑
* `feat(company)` CompanyOverview 신호등 캐시(AI signals) 연동 및 테스트 추가
* `fix(company)` CompanyOverview metrics level에 ACTUAL signal_color 반영
* `feat(company)` CompanyOverview 건강점수 캐시 연동 및 테스트 통과
* `feat(company)` CompanyOverview에 예측 캐시 연동 및 테스트 정합화
* `feat(company)` CompanyOverview 예측 캐시 서비스 추가 및 테스트 통과
* `fix(company)` CompanyOverview에서 quarterKey 미지정 시 ACTUAL 최신 분기를 기본값으로 사용
* `feat(post)` 게시글 상세 응답에 첨부파일 목록과 다운로드 정보 포함
* `fix(file)` 게시글 첨부파일 다운로드 권한을 로그인 사용자 전체로 완화하고 서비스 테스트 보강
* `feat(company)` CompanyOverview 임시 컨트롤러 추가 및 테스트 통과
* `fix(company)` 6자리 분기키 입력 정규화 및 CompanyInfoService 테스트 추가
* `fix(company)` CompanySectorService 트랜잭션 추가로 LazyInitialization 방지
* `fix(company)` 개요 지표 최신 버전 조회 시 위험/비위험 지표 최신 버전 병합 조회
* `fix(company)` CompanyOverview metricSeries 범위/데이터 소스 조정 및 metrics 다음 분기 PREDICTED+signal_color 매핑
* `feat(company)` 인사이트 기반 외부 건강도 동기화 및 CompanyInfo 평판 점수 폴백 적용
* `feat(insight)` 기업 인사이트 응답에 평균 점수(averageScore) 포함
* `feat(company)` CompanyOverviewService 추가 및 테스트 통과
* `feat(company)` CompanyOverview DTO/하위 DTO 및 테스트 추가
* `feat(metric)` 키 메트릭 엔티티/리포지토리 추가 및 H2 스키마 정합화, 리포지토리 테스트 통과
* `feat(metric)` key_metric_descriptions 시드 마이그레이션 추가 (MySQL/H2)
* `feat(company)` CompanySectorService 추가 및 테스트
* `feat(company)` CompanySectorDto 추가
* `feat(company)` CompanyInfoDto 추가 및 테스트
* `feat(company)` CompanyInfoDto id/stockCode 필드 변경
* `feat(company)` CompanyInfoService 추가 및 테스트
* `fix(metric)` company_key_metrics ai_comment 타입 정합화
* `fix(metric)` key_metric_descriptions/metric_descriptions 텍스트 타입 정합화(H2 포함)

### 2026-02-06

* `fix(test)` test 프로필 Flyway 활성화 및 H2 마이그레이션 정합화(중복 V16 정리, report_contents summary 타입 TEXT 보정) 후 `./gradlew test` 통과
* `feat(post)` 보드 기반 게시글 시스템 리팩토링 완료: `/api/posts/{categoryName}` 경로 적용, 관리자 API(`AdminPostController`) 분리, `PostService` 통합 로직 구현 및 관련 테스트 27건 통과 #70
* `fix(insight)` 인사이트 폴링 시 첫 응답 빈 배열 문제 해결: CompanyInsightService.getInsights @Transactional 제거 및 컨트롤러 테스트 통과
* `fix(insight)` 인사이트 폴링 시 첫 응답 빈 배열 문제 해결: CompanyInsightService.getInsights @Transactional 제거 및 컨트롤러 테스트 통과
* `fix(report)` PDF/지표 입력 분리 및 미발행 버전 재사용 로직 적용(수동/엑셀/예측 모두), `/api/admin/reports/metrics` JSON 수동 적재 엔드포인트 및 콘솔 갱신, 관련 테스트 통과
* `fix(report)` 최신 지표 조회 시 ACTUAL/PREDICTED가 있는 최신 버전으로 조회하도록 보정 및 관련 테스트 통과
* `fix(watchlist)` 최신 버전 조회에서 published 조건 제거 및 관련 테스트 통과
* `fix(report)` PDF/지표 입력 분리 및 미발행 버전 재사용 로직 적용(수동/엑셀/예측 모두), `/api/admin/reports/metrics` JSON 수동 적재 엔드포인트 및 콘솔 갱신, 관련 테스트 통과
* `fix(report)` 최신 지표 조회 시 ACTUAL/PREDICTED가 있는 최신 버전으로 조회하도록 보정 및 관련 테스트 통과
* `fix(watchlist)` 최신 버전 조회에서 published 조건 제거 및 관련 테스트 통과
* `test(watchlist)` 워치리스트 등록 API 컨트롤러 테스트 추가 및 통과
* `feat(watchlist)` 워치리스트 분기별 지표 값(ACTUAL, 최신 발행 버전) 조회 서비스/DTO/쿼리 추가 및 테스트 통과
* `feat(watchlist)` 워치리스트 지표 값 조회 API/보안 경로/컨트롤러 테스트 추가 및 통과
* `feat(watchlist)` 워치리스트 지표 값 범위 조회(분기 그룹) 서비스/쿼리 추가 및 테스트 통과
* `feat(watchlist)` 워치리스트 지표 값 범위 조회 API 파라미터 지원 및 컨트롤러 테스트 통과
* `feat(watchlist)` 워치리스트 분기별 지표 값(ACTUAL, 최신 발행 버전) 조회 서비스/DTO/쿼리 추가 및 테스트 통과
* `feat(watchlist)` 워치리스트 지표 값 조회 API/보안 경로/컨트롤러 테스트 추가 및 통과
* `feat(watchlist)` 워치리스트 지표 값 범위 조회(분기 그룹) 서비스/쿼리 추가 및 테스트 통과
* `feat(watchlist)` 워치리스트 지표 값 범위 조회 API 파라미터 지원 및 컨트롤러 테스트 통과

### 2026-02-07

* `chore(build)` spring-boot-devtools 의존성 추가
* `fix(swagger)` @CurrentUser 파라미터 숨김 및 파일 업로드 multipart/form-data 적용

### 2026-02-05

* `feat(report)` 보고서 PDF 단독 업로드 서비스 추가(기업 미존재 시 스킵) 및 테스트 통과
* `feat(report)` 보고서 PDF 단독 업로드 API(`/api/admin/reports/pdf`) 추가 및 테스트 통과
* `feat(watchlist)` 워치리스트 기업 집합 기준 분기별 비위험 지표 평균 조회 API(`/api/watchlists/metric-averages`) 추가 (ACTUAL + 최신 발행 버전 기준), 보안 규칙/Swagger 반영 및 관련 테스트·전체 테스트 통과
* `fix(news)` AI 뉴스 datetime 포맷 혼재(오프셋 포함/미포함) 대응: 응답 DTO는 문자열로 수신하고 서비스에서 유연 파싱(OffsetDateTime 우선, LocalDateTime fallback) 적용
* `fix(news)` AI 뉴스 응답의 무오프셋 datetime(`yyyy-MM-dd'T'HH:mm:ss.SSSSSS`) 파싱 실패 대응: DTO를 LocalDateTime으로 조정하고 NewsClient 디코딩 예외 메시지를 원인별로 분리
* `docs(news)` NewsController Swagger 문서 보강(bearerAuth, 200/401/404 응답 명세)
* `feat(company)` companies에 industry_codes 외래키를 연결하는 마이그레이션 추가 (V15, MySQL/H2) - `industry_code_id` 컬럼/인덱스/FK(`ON DELETE SET NULL`)
* `fix(watchlist)` 위시리스트 중복 등록 정책을 restore에서 충돌 반환으로 변경하고 `WATCHLIST_DUPLICATE`(409, '중복저장입니다.') 에러 코드 추가
* `test(watchlist)` 중복 등록 시 watchlist 전용 에러 코드 반환 검증 테스트 추가

### 2026-02-05

* `feat(metric)` 저장된 전체 분기를 순회해 `metric_averages`를 계산/저장하는 `MetricAverageBatchService` 추가
* `test(metric)` 전체 분기 순회 배치 실행 시 분기별 평균 저장을 검증하는 테스트 추가

### 2026-02-05

* `feat(risk)` 모든 기업-분기의 최신 보고서 버전을 대상으로 `risk_score_summaries`를 배치 계산/저장하는 `RiskScoreBatchService` 추가 (페이지 처리)
* `test(risk)` 배치 저장 결과에서 최신 버전 선택 및 `risk_level`(DANGER/CAUTION) 저장 검증 테스트 추가

### 2026-02-05

* `feat(qna)` Q&A API 사용자/관리자 분리 구현, 동적 상태(pending/answered) 계산 적용 및 통합 테스트 완료 (스키마 변경 없음) #63

* `chore(db)` 산업 코드 관련 마이그레이션(V8, V9)을 V1__init.sql로 통합 (버전 충돌 방지)
* `feat(qna)` Q&A API 사용자/관리자 분리 구현, 동적 상태(pending/answered) 계산 적용 및 통합 테스트 완료 (스키마 변경 없음) #63

### 2026-02-04

* `feat(industry)` 산업 코드 엔티티/리포지토리 추가 및 마이그레이션(V8) 초안 반영
* `fix(industry)` industry_name NULL 허용 반영 (V8)
* `feat(industry)` 업종 코드 시딩 마이그레이션(V9) 추가
* `test(industry)` 업종 리포지토리 조회 및 NULL 허용 테스트 추가
* `docs(erd)` industry_codes 관계 반영

### 2026-02-05

* `fix(swagger)` OpenAPI 서버 URL을 절대 URL 주입 방식에서 상대 경로(`/`) 고정으로 변경하여 HTTPS 환경의 Mixed Content/CORS 이슈를 예방
* `test(swagger)` `OpenApiConfigTest` 추가로 OpenAPI servers 값이 `/`로 유지되는지 단위 테스트 검증
* `test` `./gradlew test` 전체 테스트 통과
* `feat(metric)` metrics 테이블에 `is_risk_indicator` 컬럼 추가(MySQL/H2 Flyway V8) 및 `MetricsEntity` 매핑 반영
* `test(metric)` 시드 기본값(false)과 컬럼 미지정 INSERT 시 DB 기본값(0) 저장을 검증하는 저장소 테스트 2건 추가
* `feat(risk)` `risk_score_summaries` 테이블(Flyway V9, MySQL/H2)과 위험도 집계 도메인/서비스 추가 (`is_risk_indicator=1` + `ACTUAL` 기준)
* `test(risk)` 위험도 계산 서비스 테스트 추가(집계 성공/집계 대상 없음 UNDEFINED) 및 `./gradlew test` 전체 통과
* `feat(metric)` `metric_averages` 테이블(Flyway V10, MySQL/H2)과 분기별 비위험 지표 통계 집계 서비스 추가 (`is_risk_indicator=0` + `ACTUAL` + 최신 버전 기준)
* `test(metric)` 비위험 지표 통계 집계 테스트 추가(최신 버전 반영, PREDICTED/위험 지표 제외) 및 `./gradlew test` 전체 통과
* `feat(watchlist)` `company_watchlists` 테이블(Flyway V11)과 watchlist 등록/삭제/대시보드 API 추가, 비위험 지표는 최신 발행 버전+ACTUAL+시장 평균 조합으로 조회
* `refactor(user)` users의 `company_id` 제거(Flyway V12) 및 `UserEntity`/보안 경로 업데이트, watchlist 대시보드 서비스 테스트 추가 후 `./gradlew test` 전체 통과

### 2026-02-04

* `feat(company)` CompanyAiService 조회용 API(`/api/companies/{companyCode}/ai-analysis`) 추가 및 ROLE_USER 보안 규칙 반영, 컨트롤러 테스트 통과
* `feat(company)` AI 리포트 PDF 생성/저장 API(`/api/companies/{companyCode}/ai-report`) 추가 및 ROLE_ADMIN 보안 규칙 반영, 컨트롤러 테스트 통과
* `feat(dev)` API 콘솔에 기업 AI 분석 조회 섹션 추가 및 기업 검색 경로(`/api/companies/search`) 정합화, 콘솔 뷰 테스트 통과
* `feat(dev)` API 콘솔에 기업 AI 리포트 PDF 생성/저장 테스트 섹션 추가
* `feat(dev)` API 콘솔 기업 AI 분석 섹션에 기업명 검색/선택 UX 보강(디바운스 검색) 및 `SPRING_PROFILES_ACTIVE=test` 기준 전체 테스트 통과
* `fix(company)` AI 서버 예측 응답 DTO(`AiAnalysisResponse`) 누락 추가 및 `ai.server.url` 설정 추가, 전체 테스트 통과
* `fix(dev)` dev 콘솔 템플릿들의 API 호출 경로를 실제 `/api/*` 컨트롤러 매핑과 정합화, 전체 테스트 통과

### 2026-02-02

* `fix(test/security)` 보고서 파일 URL 조회 테스트 경로(`/api`) 정합화 및 `/api/reports/files/*/url` ROLE_USER 보안 규칙 추가, 전체 테스트(183개) 통과
* `refactor(web)` 모든 컨트롤러 엔드포인트에 `/api` 접두사 적용 및 보안 설정 동기화
* `refactor(auth)` 회원가입 DTO (`SignupRequest`) 필드명 수정 (`username` -> `name`) 및 관련 서비스 수정
* `test(integration)` API 경로 및 DTO 변경에 따른 통합 테스트 코드 수정 및 정상화 (전체 177개 통과)
* `refactor(security)` 기업 검색 API 경로를 `/companies/search`로 이동하고 ROLE_USER 전용 보안 규칙 적용
* `test(company)` 기업 검색 API 인증/권한(401/403/200) 통합 테스트 추가
* `refactor(security)` 보고서 지표 조회 API 경로를 `/reports/metrics/*`로 이동하고 ROLE_USER 전용 보안 규칙 적용
* `test(report)` 보고서 지표 조회 API 인증/권한(401/403/200) 통합 테스트 추가
* `refactor(security)` 보고서 PDF 다운로드 경로를 `/reports/files/{id}`로 이동하고 ROLE_USER 전용 보안 규칙/다운로드 URL 경로 반영
* `test(report)` 보고서 PDF 다운로드 API 인증/권한(401/403/302) 통합 테스트 추가
* `feat(auth)` 이메일 인증 리다이렉트 상태코드 매핑 및 returnUrl 화이트리스트 검증 추가
* `test(auth)` 이메일 인증 리다이렉트/returnUrl 검증 WebMvc 테스트 보강
* `feat(auth)` 이메일 인증 결과 DTO 추가 및 redirect=false JSON 응답 정비

### 2026-02-02

* `feat(auth)` 이메일 인증 리다이렉트 상태코드 매핑 및 returnUrl 화이트리스트 검증 추가
* `test(auth)` 이메일 인증 리다이렉트/returnUrl 검증 WebMvc 테스트 보강
* `feat(auth)` 이메일 인증 결과 DTO 추가 및 redirect=false JSON 응답 정비

### 2026-01-30

* `fix(dev)` 예측 보고서 콘솔 토큰 로딩 수정
* `fix(security)` 예측 보고서 콘솔 경로 접근 허용
* `feat(dev)` 예측 보고서 조회 콘솔 페이지 추가
* `feat(report)` 최신 예측값 조회 API/보고서 PDF 다운로드 추가
* `feat(report)` 최신 예측값 조회 로직/테스트 추가
* `feat(dev)` API 콘솔 보고서 발행 테스트 폼 추가
* `feat(report)` 보고서 지표+PDF 발행 API/테스트 추가
* `feat(report)` 보고서 지표+PDF 발행 서비스/테스트 추가
* `feat(dev)` API 콘솔 예측값 적재 UI 추가
* `feat(dev)` API 콘솔 기업 검색 결과 디바운싱 리스트 표시
* `feat(report)` 보고서 지표 예측값 적재 API/테스트 추가
* `feat(report)` 보고서 지표 예측값 적재 서비스/DTO 및 테스트 추가
* `fix(report)` 기업코드 정규화 로직 보완(비숫자 포함도 6자리 미만이면 좌측 패딩)
* `chore(logging)` 비동기 콘솔/파일 로깅 및 롤링 정책 설정
* `feat(report)` 보고서 지표 최신 버전 조회 리포지토리/테스트 추가
* `feat(report)` 보고서 지표 조회 서비스/DTO 및 테스트 추가
* `feat(report)` 보고서 지표 분기별 그룹핑 응답 추가
* `feat(report)` 보고서 지표 그룹 조회 API/문서/테스트 추가
* `feat(dev)` API 콘솔에 보고서 지표 그룹 조회 버튼 추가
* `feat(company)` 기업명 검색 서비스/레포지토리 및 테스트 추가
* `feat(company)` 기업 검색 API/문서/콘솔 연동 추가
* `fix(auth)` Auth Console 로그아웃 버튼 동작 보강
* `test(auth)` 로그아웃 테스트 Role 중복 저장 보정
* `fix(auth)` 로그아웃 전체 처리 토큰 검증(AccessTokenValidator) 반영 및 테스트 추가
* `fix(auth)` 단일 로그아웃 Access 토큰 블랙리스트 및 로그아웃 테스트 보강
* `feat(auth)` Auth Console 보호 API UX 개선
* `fix(test)` 테스트 프로파일 정리 및 엑셀 파서 테스트 메모리 워크북 전환
* `chore(build)` spring-batch-core 의존성 명시 추가
* `refactor(dto)` DTO MapStruct 전환 및 매퍼 주석 보강
* `refactor(company)` DART 동기화 응답 매핑 MapStruct 적용
* `refactor(report)` 보고서 그룹/예측 응답 MapStruct 매핑 추가
* `chore(config)` dev 전용 SQL 로그/Turnstile 기본값 분리
* `chore(config)` dev/prod DB 환경변수 체계 통일
* `docs(config)` env 예시/운영 env 템플릿 정합화
* `chore(config)` prod 이메일 인증 skip=false 유지 및 dev 설정 복구
* `chore(config)` prod 이메일 설정 import 추가
* `docs(config)` .env.example 환경변수 목록 정리
* `docs(config)` README 환경변수 요약 추가

### 2026-01-31

* `chore(deploy)` prod 프로필에서 Redis만 docker compose로 실행
* `chore(config)` prod 파일 업로드 크기 바인딩 분리(FILE_MAX_SIZE_BYTES)
* `feat(file)` files storage_key 컬럼 추가 및 저장 메타데이터 보강
* `feat(file)` S3 presigned 다운로드 리졸버 추가
* `test(file)` 다운로드 presigned 리다이렉트 통합 테스트 추가
* `fix(security)` actuator health 접근 허용
* `test(security)` 헬스체크 접근 테스트 추가
* `chore(auth)` 리프레시 토큰 쿠키 SameSite/secure 설정 분리

### 2026-02-01

* `fix(security)` 공개 경로 축소 및 로그아웃 인증 요구 반영
* `feat(auth)` 이메일 인증 리다이렉트 옵션 추가
* `chore(dev)` 인증 콘솔 경로 /dev/*로 정리 및 템플릿 링크 갱신
* `refactor(dev)` 인증 콘솔 컨트롤러 DevConsoleController로 통합
* `refactor(dev)` dev 카테고리 API를 DevConsoleController로 통합
* `fix(web)` favicon 요청 처리 추가
* `feat(file)` 파일 다운로드 URL 조회 API(/files/{id}/url) 및 테스트 추가
* `feat(category)` 카테고리 조회 API(/categories) 추가 및 테스트 보강
* `fix(security)` 카테고리 조회 공개 

## 3. Known Issues (알려진 문제/보류 사항)

- 없음

## 4. History (이전 기록)

- 2026-06-23 | 작업: Spring Modulith 도입 및 패키지 구조 재정리, 전체 테스트 통과 | 결과: Bounded Context 캡슐화 및 단방향 의존성 정합성 입증, 다이어그램 자동화 생성 및 416여 개 테스트 그린패스 달성 | 이슈: 없음
- 2026-05-26 | 작업: 포트폴리오 가이드 및 README.md 2차 하드닝 | 결과: 대규모 트래픽의 본질(RPS 수치가 아닌 SLO 수립, P95/P99 꼬리 지연 사수, 디그레이드 모드 융합)을 포트폴리오 가이드에 반영하고 README.md 성능 테이블 고도화 완료 | 이슈: 없음
- 2026-05-26 | 작업: 포트폴리오 치트 시트(PORTFOLIO_GUIDE.md) 신설 및 README.md 시각화 보완 완료 | 결과: 3PASS 구조의 포트폴리오 가이드라인 신설 및 README.md 내 성능 테이블/Mermaid Gantt 차트 시각화 추가 완료 | 이슈: 없음
- 2026-05-26 | 작업: Python Mock AI Worker 분리 및 Java Spring Kafka Saga 이종 연동 완결 | 결과: Python FastAPI 워커 분리, fpdf2 실물 PDF 공유 스토리지 저장 및 Kafka 양방향 콜백 파파이라인 구축 성공 | 이슈: 없음
- 2026-05-22 | 작업: README.md 실제 데이터베이스 인덱스 4대 최적화 사례 및 Fallback 트러블슈팅 분석 문서화 | 결과: 포트폴리오용 README.md 상단 Executive Summary 표에 5번째 사례 추가 및 Backend Leader 파트 갱신 | 이슈: 없음
- 2026-05-22 | 작업: README.md에 인프라 보안 및 SWOT/VRIO 관점 비즈니스 가치 분석 반영 완료 | 결과: A안에 따라 AWS 클라우드/듀얼 클라우드 설계 및 SWOT/VRIO 기반 분석 결과 대섹션 신설 통합 완료 | 이슈: 없음
- 2026-05-22 | 작업: README.md 면접관 최적화 리오더링 및 기여도 격상 완결 | 결과: 기여도를 최상단 단독 격상 배치하고 아키텍처/트러블슈팅 전면 배치 완료 | 이슈: 없음
- 2026-05-21 | 작업: 포트폴리오 비즈니스 최적화 README.md 개편 완료 | 결과: Spring Batch 단축, Redis 프리워밍, Kafka 격리 등 비즈니스 임팩트 전면 반영 완료 | 이슈: 없음
- 2026-05-21 | 작업: Transactional Outbox & Custom Saga 2차 융합 | 결과: SKIP LOCKED 기반 Outbox 및 역순 보상 트랜잭션 성과를 시퀀스 흐름도 및 사례로 통합 완료 | 이슈: 없음
- 2026-03-04 | 작업: CI YAML 검증 및 Docker 빌드 실패 보정 완료 | 결과: 중복 키 fail-fast 파서 탑재, bootstrap profiles CI 고정 및 Docker JAR 단독 패키징 정합화 완료 | 이슈: 없음
- 2026-02-26 | 작업: AI 리포트 LazyInitializationException 해결 및 DB 세션 최적화 반영 | 결과: FileId 직접 조회로 `no session` 제거 및 storeAndLinkReport 트랜잭션 격리로 외부 호출 분리 완료 | 이슈: 없음
- 2026-02-25 | 작업: 인사이트 트랜잭션 경계 분리 및 조회 오케스트레이션 정리 | 결과: 쓰기/읽기 단계 분리 및 API 호출 순서 정리로 LazyInitializationException 완화 완료 | 이슈: 없음
- 2026-02-18 | 작업: CompanyOverview ACTUAL 부재 fallback 및 Virtual Thread 토글 환경변수 보강 | 결과: 분기 데이터 누락 시 빈 개요 응답 fallback 적용 및 compose/env에 Virtual Thread 변수 추가 완료 | 이슈: 없음
- 2026-02-17 | 작업: ValidateService compose 필수 검증 및 docker command image env 로드 보정 | 결과: 헬스체크 스크립트 compose ps/logs ECR 이미지 로드 정합화 및 Validate 단계 compose-app 유효성 검사 추가 완료 | 이슈: 없음
- 2026-02-16 | 작업: 이슈 #127 AI 장시간 작업 Kafka 비동기 처리 도입 및 10종 테스트 정합화 | 결과: 디스패처/컨슈머 연동 및 Watchlist 코멘트 비동기 이관 완료, Auth CSRF 테스트 회귀 보정 완료 | 이슈: 없음
- 2026-02-16 | 작업: 이슈 #125 AI 타임아웃 503 전역 예외 처리 및 인사이트 외부 장애 fallback 보강 | 결과: 타임아웃 상향, `ExternalAiUnavailableException` 기반 503 매핑 및 기존 DB 인사이트 캐시 최우선 재사용 fallback 적용 완료 | 이슈: 없음
- 2026-02-14 | 작업: QnA 사용자명 마스킹, 로그인 실패 임계치 잠금 및 외부 AI Resilience4j 서킷브레이커 적용 | 결과: API 로깅 JWT/Cookie 이중 마스킹 적용, HMAC-SHA256 pepper 해시 전환, resend-verification API 레이트리밋/보안 정규화 완료 | 이슈: 없음
- 2026-02-13 | 작업: Docker MVP 전환 및 CodeBuild ECR 푸시/듀얼 런타임 deploy-runtime 스크립트 리팩토링 | 결과: compose 기반 app/redis 기동 구축, buildspec.yml ECR/plain bootJar 복사 및 systemd/docker 듀얼 런타임 배포 안전망 보강 완료 | 이슈: 없음
- 2026-02-12 | 작업: role 시드 멱등 고정 및 signup duplicate 409 예외 전환, report row lock 공통 버전 발급 통합 | 결과: V26 seed 적용, UserDomainService 동적 생성 제거, `GetOrCreateResolver` 경합 충돌 재조회 패턴 통일 및 기존 PDF 재사용 `PROCESSING` 스킵 최적화 완료 | 이슈: 없음
- 2026-02-11 | 작업: Virtual Thread Phase1 도입 및 JMeter 벤치마크/Actuator metrics 자동화 구축 | 결과: insight/email VT 토글, perf mock 모드, JMeter 벤치마크 및 런타임 리포트 자동 비교 파이프라인 완성, 비관적 락을 통한 AI 리포트 동시성 충돌 완화 완료 | 이슈: 없음
- 2026-02-11 | 작업: logout-all refresh token Redis/DB 일괄 폐기 및 refresh 검증 보강 | 결과: 전체 로그아웃 시 refresh 토큰 즉각 무효화 및 레거시 세션 타임아웃 호환 처리 완료 | 이슈: 없음
- 2026-02-11 | 작업: user list ROLE_ADMIN 제외 및 test JWT RSA 키 고정 | 결과: `NOT EXISTS` 기반 쿼리로 관리자 노출 차단 및 CI test profile JWT RSA 키 파일 로컬 고정으로 빌드 context 안정화 완료 | 이슈: 없음
- 2026-02-10 | 작업: CI test profile 안정화, companies FULLTEXT index 마이그레이션 분리 및 컨트롤러 REST 정규화 | 결과: dev->test 프로필 통일, 하이브리드 fallback 적용, 복합 매처 URI 호환 적용, Swagger endpoint 마이그레이션 명세 완료 | 이슈: 없음
- 2026-02-09 | 작업: 워치리스트 소프트 삭제 중복 복구, AI 코멘트 캐시 저장 및 수동 적재 API 구현 | 결과: 삭제 이력 복구 restore 패턴 정합화, 워치리스트 등록 시 AI 코멘트 선행 캐싱 연동 및 뉴스 재수집 후 external_health_score 즉시 동기화 연결 완료 | 이슈: 없음
